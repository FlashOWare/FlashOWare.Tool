namespace FlashOWare.Tool.Cli.Tests.IO;

internal sealed class VisualBasicFileProvider
{
    private readonly DirectoryInfo _directory;

    public VisualBasicFileProvider(DirectoryInfo directory)
    {
        _directory = directory;
    }
}
